import { BeautifyKeyStringPipe } from './beautify-key-string.pipe';

describe('BeautifyKeyStringPipe', () => {
  it('create an instance', () => {
    const pipe = new BeautifyKeyStringPipe();
    expect(pipe).toBeTruthy();
  });
});
